import $ from "jquery";

$(document).ready(function(){
  $('.product-image-slider').slick({
    arrows: false,
    dots: true
  });

  $('.quarter-hero-slider').slick({
    arrows: false,
    dots: true
  });
});