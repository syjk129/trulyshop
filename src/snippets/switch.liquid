{%- comment -%}
  Switch Component

  Accepts:
  - value_id: {String} unique id for the switch component
  - default:
{%- endcomment -%}
<label class="switch">
  <input type="checkbox" id="{{ value_id }}">
  <span class="slider"></span>
</label>